# Benchmark package

The benchmark package will hold provider-neutral run specifications, metric calculations, pricing abstractions, and reproducibility helpers. Phase 1 reserves this boundary without creating fake results or a premature execution engine.

